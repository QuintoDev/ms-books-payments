image: maven:3.9.9

pipelines:
  default:
   - step: 
       name: Install dependencies
       services:
        - docker
       caches:
        - maven
       script:
        - apt-get update && apt-get install -y docker.io