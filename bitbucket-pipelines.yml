image: maven:3.3.9

pipelines:
  default:
  - step:
      name: 'Maven Install'
      script:
          - echo "Run Maven install"
          - mvn install
  - step:
      name: 'Maven Clean & Package'
      script:
          - echo "Run Maven Clean & Package"
          - mvn clean package