image: maven:3.3.9

pipelines:
  default:
    - parallel:
      - step:
          name: 'Maven Clean & Package'
          script:
            - echo "Run Maven Clean & Package"
            - mvn clean package