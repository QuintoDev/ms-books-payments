name: Hello World Workflow

on: 
  push:
    branches:
      - main  # Ejecutar el workflow cuando se haga un push a la rama "main"
  pull_request:
    branches:
      - main  # Ejecutar también en PR hacia la rama "main"

jobs:
  hello-world:
    runs-on: ubuntu-latest  # Elige el sistema operativo para el runner

    steps:
      # Paso 1: Checkout del repositorio
      - name: Checkout code
        uses: actions/checkout@v3

      # Paso 2: "Hola Mundo"
      - name: Print Hello World
        run: echo "Hola, ¡este es mi primer workflow en GitHub Actions! 🎉"