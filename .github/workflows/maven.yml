name: Java CI with Maven

on: 
  push:
    branches:
      - dev

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run a docker test
        run: docker